{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "playwright:chromium": "npx playwright test --project=chromium",
    "playwright:firefox": "npx playwright test --project=firefox",
    "playwright:webkit": "npx playwright test --project=webkit",
    "playwright:all": "npx playwright test",
    "playwright:ui": "npx playwright test --ui",
    "playwright:debug": "npx playwright test --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.11.0",
    "eslint-plugin-playwright": "^0.21.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
